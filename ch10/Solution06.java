public class Solution06{
	//To sort a file of 20GB size, we should use external sort, every time the program read in string of size X, where X is the size of the memory
	//We sort the read string and save them back to the file system. And then we merge each sorted chunk one by one.

}
